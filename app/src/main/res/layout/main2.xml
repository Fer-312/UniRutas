<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    tools:context=".ui.main.main2">

    <!-- Título -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="UniRutas - Prueba de Conexión"
        android:textSize="24sp"
        android:textStyle="bold"
        android:gravity="center"
        android:layout_marginBottom="24dp"
        android:textColor="@color/black" />

    <!-- Botón para probar conexión -->
    <Button
        android:id="@+id/btnTestConnection"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Probar Conexión con Supabase"
        android:textSize="16sp"
        android:layout_marginBottom="16dp"
 />

    <!-- Botón para obtener rutas -->
    <Button
        android:id="@+id/btnGetRutas"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Obtener Rutas"
        android:textSize="16sp"
        android:layout_marginBottom="16dp"
/>

    <!-- Botón para obtener estudiantes -->
    <Button
        android:id="@+id/btnGetEstudiantes"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Obtener Estudiantes"
        android:textSize="16sp"
        android:layout_marginBottom="24dp"
/>

    <!-- Progress Bar -->
    <ProgressBar
        android:id="@+id/progressBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:visibility="gone"
        android:layout_marginBottom="16dp" />

    <!-- TextView para mostrar resultados -->
    <TextView
        android:id="@+id/tvResult"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:text="Presiona un botón para probar la conexión..."
        android:textSize="14sp"
        android:padding="12dp"

        android:scrollbars="vertical"
        android:isScrollContainer="true" />

    <!-- Contador de resultados -->
    <TextView
        android:id="@+id/tvCount"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Resultados: 0"
        android:textSize="12sp"
        android:textStyle="italic"
        android:gravity="end"
        android:layout_marginTop="8dp" />

</LinearLayout>